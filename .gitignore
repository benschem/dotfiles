# MacOS system files
.DS_Store
.Trashes
Icon? # The Icon\r file used by Finder (trailing CR)

# Editor and tools
.bundle/
.nvm/
.npm/
.cache/

# Don't commit SSH config secrets
.ssh/config

# Your real gitconfig
.gitconfig

**/.claude/settings.local.json

# Claude Code: never track secrets/state if more of ~/.claude ever gets symlinked in
.claude/.credentials.json
.claude/history.jsonl
.claude/projects/
.claude/sessions/
